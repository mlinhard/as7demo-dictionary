AS7 Dictionary Extension Demo

this is an example of an AS7 module.
it does nothing more than storing simple key, value pairs
loads the list of the pairs from the persistent config and stores them.
it also contains MBean that can list all the entries in the dictionary.

Installation

mvn install
cd scripts
edit JBOSS_HOME in install.sh
install.sh
